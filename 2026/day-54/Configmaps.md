Day 54 — Quick Summary
Created ConfigMaps from literals and files.
Used ConfigMaps as environment variables and volume-mounted files.
Created Secrets for database credentials.
Injected Secrets into Pods using secretKeyRef and volumes.
Learned that Base64 is encoding, not encryption.
Tested ConfigMap updates and learned:
Volume-mounted values update automatically.
Environment variables require a Pod restart.
Cleaned up Pods, ConfigMaps, and Secrets.

Key takeaway: ConfigMaps store non-sensitive configuration, while Secrets are intended for sensitive values.

I can also provide a short day-54-configmaps-secrets.md ready for submission.
